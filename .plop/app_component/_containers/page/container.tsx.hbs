import 'server-only'

import { {{componentName}}PagePresentation } from './presentation'

/**
 * {{componentName}}PageContainer
 * @description Server Components上でのデータフェッチなどのサーバーサイド処理を行うコンテナコンポーネント
 */
export const {{componentName}}PageContainer = () => {
  return <{{componentName}}PagePresentation />
}
